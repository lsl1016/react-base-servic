# Prometheus 抓取配置：仅采集同 compose 网络内的业务服务，全程内网零公网流量。
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: react-base-service
    metrics_path: /metrics
    static_configs:
      - targets: ["service:8080"]
