# 从 /var/lib/grafana/dashboards 目录自动加载面板 JSON。
apiVersion: 1

providers:
  - name: react-base-service
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
