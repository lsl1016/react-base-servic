# 基础框架配置（日志、HTTP 服务端口）
pprof:
  enable: false

log:
  level: info
  stdout: true
  # 文件落盘与轮转（写入容器 service_logs 卷）
  dir: "log"
  filename: "app"
  max_size_mb: 100
  max_age_days: 7
  max_backups: 10

server:
  address: ":8080"
